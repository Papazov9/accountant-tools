spring:
    datasource:
        url: jdbc:mysql://localhost:3307/accountantdb?createDatabaseIfNotExist=true
        username: accountant
        password: accountantpassword
        driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQLDialect